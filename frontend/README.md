# Frontend

A minimal React frontend for the ecommerce project.

## Run locally

1. Install dependencies:

```powershell
cd "c:\Users\Admin\OneDrive\Desktop\ecommerce\ecommerce\frontend"
npm install
```

2. Start the app:

```powershell
npm start
```

The app proxies API requests to `http://localhost:5000`.
